import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // reactStrictMode: false
  // async redirects() {
  //   return [
  //     {
  //       source: "/", // Match all routes
  //       destination: "/", // Redirect to /kopken
  //       permanent: false, // Temporary redirect
  //     },
  //   ];
  // },
};

export default nextConfig;
